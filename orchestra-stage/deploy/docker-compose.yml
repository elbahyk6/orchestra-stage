services:
  backend:
    build:
      context: ../app
      dockerfile: ../dockerfiles/Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ../app:/app
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - ../.env